import styled from "styled-components";

export const HomeWrapper = styled.main`

    .list__posts{
        display: flex;
        flex-wrap: wrap;
        gap: 24px;
        justify-content: center
    }
`